server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-org/mobile-skins-config
          default-label: main
  application:
    name: config-server
  profiles:
    active: native
    
